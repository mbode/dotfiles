name: Home Install

on: [push]

jobs:
  build:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: "recursive"
    - name: Execute home install
      run:  ./install home
