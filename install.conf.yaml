- defaults:
    shell:
      stdout: true
      stderr: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.gitignore:
    ~/.zshrc:
    ~/Brewfile:
    ~/.gradle/gradle.properties:
      path: gradle.properties
      create: true

- shell:
  - command: git submodule update --init --recursive
    description: Install submodules
  - command: ./brew.sh
    description: Install homebrew if not present
  - command: brew bundle
    description: Install homebrew recipes
  - command: brew update
    description: Update homebrew recipes
  - command: brew upgrade
    description: Upgrade homebrew recipes
  - command: pip install -r requirements.txt
    description: Install python packages
  - command: pip3 install -r requirements3.txt
    description: Install python3 packages
  - command: ./zsh.sh
    description: Enable zsh if necessary
